<Application
    x:Class="SimpleViewer.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- 职责：全局视觉样式（视觉对齐 demo）——Ghost 按钮（透明底 hover 浅底）、
              标签树行按钮（组行/标签行：透明底 + hover 浅叠加 + 激活行浅灰高亮分层叠加）。
              约束：模板仅 Border/Grid/ContentPresenter + VisualStateManager，不使用字体图标
              （FontIcon/SymbolIcon/Glyph 一律不用），不含 U+00AB/U+00BB 与 PUA 字形（XamlCompiler 规避）。 -->
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Chrome 面板不透明底（2026-09-23 走查 3 拍板：工具栏/左右栏统一不透明——半透明
                 LayerFillColorDefault 在单图大图上让按钮不可读；深色值取自原 Layer 底在根底色上
                 的实测混合值 #27272E（观感不变），浅色取同族 #F3F3F8。仅用于 chrome 表面，
                 画布/卡片等不引用。自定义键规避「文档存在但 WinUI 3 实际缺失」的 ThemeResource 风险
                 （LayerFillColorSecondaryBrush 事故先例）。 -->
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Dark">
                    <SolidColorBrush x:Key="ChromeBackgroundBrush" Color="#27272E" />
                </ResourceDictionary>
                <ResourceDictionary x:Key="Light">
                    <SolidColorBrush x:Key="ChromeBackgroundBrush" Color="#F3F3F8" />
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>

            <!-- ghost 按钮（demo .ghost-btn）：透明底 + hover 主题浅底；文字按钮通用 -->
            <Style x:Key="GhostButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Padding" Value="10,5" />
                <Setter Property="CornerRadius" Value="6" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border
                                x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                Padding="{TemplateBinding Padding}">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="Root.Background" Value="{ThemeResource SubtleFillColorSecondaryBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="Root.Background" Value="{ThemeResource SubtleFillColorTertiaryBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Target="Root.Background" Value="Transparent" />
                                                <Setter Target="Content.Opacity" Value="0.4" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <ContentPresenter
                                    x:Name="Content"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ghost 图标位小按钮（demo .icon-btn）：透明底 hover 浅底，次要色符号字符（＋⇄✎✕） -->
            <Style
                x:Key="GhostIconButtonStyle"
                BasedOn="{StaticResource GhostButtonStyle}"
                TargetType="Button">
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Padding" Value="5,2" />
                <Setter Property="CornerRadius" Value="4" />
            </Style>

            <!-- 图库右栏选中集并集 chip（batch-tag-management Step 4）：圆角矩形（CornerRadius 5，
                 区别于单图右栏全圆胶囊）+ 安全键底色/描边；内容 = 标签名（12）+ 选中集内计数（11 次要灰）
                 + 内嵌 ✕ 按钮（GhostIconButtonStyle，控件 XAML 内逐 chip 内联）。
                 契约：chip 本体无 Command 无 Click handler——点 chip 无副作用（不筛选不打标，
                 PRD 右栏 chip 与左栏不同），仅 ✕ 移除；嵌套 Button 点击各自直达不冒泡
                 （GroupHeaderButtonStyle 组行内嵌管理按钮同先例）。 -->
            <Style
                x:Key="SelectionChipStyle"
                BasedOn="{StaticResource GhostButtonStyle}"
                TargetType="Button">
                <Setter Property="Background" Value="{ThemeResource SubtleFillColorSecondaryBrush}" />
                <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="5" />
                <Setter Property="Padding" Value="9,3" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="UseSystemFocusVisuals" Value="False" />
            </Style>

            <!-- 瀑布流卡片按钮（cr/P1-6：卡片主交互 Click 化——Tapped 对键盘/UIA 静默失效，
                 RULE「交互用 Click 不用 Tapped」）：模板仅 Border + ContentPresenter（对齐 Ghost 系
                 透明底极简模板），CornerRadius 10 对齐卡片圆角，底色/描边由卡片实例的 Background/
                 BorderBrush 绑定提供；hover/选中视觉沿用既有 code-behind Translation 与 x:Bind 绑定，
                 模板不叠加 PointerOver 底色（保持改造前像素级一致）。拖拽 CanDrag + DragStarting 与
                 Click/DoubleTapped 照常共存（系统拖拽需按住位移超阈值才进入）。 -->
            <Style x:Key="CardButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="VerticalContentAlignment" Value="Stretch" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border
                                x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                Padding="{TemplateBinding Padding}">
                                <ContentPresenter
                                    x:Name="Content"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 标签组组头按钮（目录树父行）：整行透明底承载展开/折叠，hover/pressed 主题浅叠加；
                 内容 Stretch 占满整行扩大点击区、行高约 36px（树行观感）；内嵌管理按钮（＋⇄✎✕）为独立
                 Button——WinUI 嵌套 Button 点击各自直达不冒泡，点管理按钮不触发展开切换 -->
            <Style x:Key="GroupHeaderButtonStyle" TargetType="Button">
                <Setter Property="HorizontalAlignment" Value="Stretch" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="MinHeight" Value="36" />
                <Setter Property="Padding" Value="6,0" />
                <Setter Property="CornerRadius" Value="6" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border
                                x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                Padding="{TemplateBinding Padding}">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="Root.Background" Value="{ThemeResource SubtleFillColorSecondaryBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="Root.Background" Value="{ThemeResource SubtleFillColorTertiaryBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Target="Root.Background" Value="Transparent" />
                                                <Setter Target="Content.Opacity" Value="0.4" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <ContentPresenter
                                    x:Name="Content"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 标签树行按钮（目录树子行，demo .tag-row）：底色 = 激活高亮（x:Bind 数据绑定提供，
                 普通行透明）；hover/pressed 经独立叠加层（HoverOverlay）加深——分层叠加保证激活行悬停时
                 高亮不丢失；DropOverlay 为拖拽打标目标的落下高亮层（默认透明零边框，code-behind 在
                 DragOver/DragLeave/Drop 切换颜色与边框，TagSidebarControl 视觉树按名回查）；
                 内容 Stretch 占满整行、行高约 34px，计数右对齐依赖整行拉伸 -->
            <Style x:Key="TagTreeRowButtonStyle" TargetType="Button">
                <Setter Property="HorizontalAlignment" Value="Stretch" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="MinHeight" Value="34" />
                <Setter Property="Padding" Value="6,0,6,0" />
                <Setter Property="CornerRadius" Value="5" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid>
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="HoverOverlay.Background" Value="{ThemeResource SubtleFillColorSecondaryBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="HoverOverlay.Background" Value="{ThemeResource SubtleFillColorTertiaryBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Target="Content.Opacity" Value="0.4" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <Border
                                    x:Name="ActiveBackground"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="{TemplateBinding CornerRadius}" />
                                <Border
                                    x:Name="HoverOverlay"
                                    Background="Transparent"
                                    CornerRadius="{TemplateBinding CornerRadius}" />
                                <Border
                                    x:Name="DropOverlay"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    CornerRadius="{TemplateBinding CornerRadius}" />
                                <ContentPresenter
                                    x:Name="Content"
                                    Margin="{TemplateBinding Padding}"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch" />
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
