# 更新日志（Changelog）

本文件记录用户可感知的变更。格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循语义化版本；历史起点为本文件引入（v1.0.0 之前的旧版单图查看器不追溯）。

## [1.0.0] - 2026-09-19

首个正式版本：从"单目录单图查看器"升级为"带标签的图库浏览器"。

### 新增

- **TagSpaces 文件名标签**：标签内嵌于文件名尾部方括号（`photo[风景 已修].jpg`），增删改全部
  通过重命名完成，不产生任何额外文件；与 TagSpaces 桌面版互通。
- **标签组体系**：互斥组（组内单选，替换对用户透明）与兼容组（组内可叠加）两种语义，
  组/标签支持新建、重命名、删除（执行前告知影响张数）；跨平台文件名长度预算预检
  （Windows 260 字符路径 / Linux 255 字节文件名双口径，超限阻止并提示）。
- **瀑布流图库**：递归扫描图库根目录、虚拟化滚动、按原始宽高比多列瀑布排布、
  缩略图按需异步加载与磁盘缓存；扫描渐进呈现（无需等全量完成），扫描中可随时操作。
- **多选与批量打标**：Explorer 心智选择（单击单选 / Ctrl 加减选 / Shift 范围 / Ctrl+A 全选）；
  拖拽图片到左侧标签行打标（拖拽集内任一卡 = 整集），拖拽视觉为小缩略图随鼠标，
  多选时悬停显示"打标 N 张"。
- **打标入口**：拖拽打标、单图详情右栏 chips（✕ 移除 / ＋ 目录选择器添加）、
  可视化快捷键打标（单图/选中集批量）。
- **标签筛选**：点标签筛选（单选替换心智，Ctrl 多标签 OR）；侧栏 ∅ 按钮一键筛选
  无标签图片（打标工作入口）；筛选条实时命中统计，chip 可逐个移除。
- **单图查看**：遮盖式布局（画布铺满整窗，侧栏/工具栏浮层遮盖）——收展侧栏图片位置
  纹丝不动、不闪不重载；滚轮锚点缩放、拖拽平移、双击复位、旋转、全屏、回收站删除、
  分类归档；左上"◀ 返回图库"按钮与 Esc/Enter 键盘路径。
- **单图详情右栏**：文件信息（文件名/大小/像素尺寸/序号）与标签 chips 管理。
- **主题三态**：深色（默认）/浅色/跟随系统，弹窗与浮层跟随。
- **启动体验**：无参数启动自动恢复上次图库根目录；CLI 支持 `viewer <文件>` 直开与目录参数。

### 性能

- 打标（重命名）不重载图片：同图改名保持旧源直到新源就绪，缓存键随路径迁移；
  成功回执静默（就地视觉反馈），仅失败弹回执（数量与原因）。
- 几十万张量级：索引（SQLite）与缩略图缓存为可丢弃缓存，打开图库清表重扫；
  解码走 WIC Fant 插值防缩放锯齿；EXIF 方向计入宽高比（竖拍卡片比例正确）；
  卡片回收释放位图内存；扫描与筛选/重开图库竞态收口。
- 同目录切图体感 < 100ms，冷启动 ≤ 2s（PRD 口径）。

### 修复与加固（CR round1）

- 扫描中重开图库/激活筛选的竞态与重复卡片问题收口。
- 卡片交互 Button 化（键盘与辅助功能路径可达）；图库 Enter 进入单图。
- 设置文件损坏（v1 迁移/非法字符）不再崩溃；标签名拒绝文件系统非法字符。
- 标签重命名部分失败、批量打标失败明细回执；诊断日志与 UI 心跳看门狗。
- 核心状态机（筛选/瀑布流布局/自然排序一致性）下沉 Core 并补自动化测试（100 项）。
- 瀑布流卡片行序回归修复：缩略图被钉死在 48px 细条（cr/P2-9 行序颠倒），恢复
  图片在上、文件名在下的正常卡片布局。
- 自包含发布包启动崩溃修复：publish 布局补齐散装 .xbf 与图标资源
  （此前报 XamlParseException 无法定位 MainWindow.xaml）。

### 构建

- 双 csproj 拆分（UI / 可单测 Core）；`scripts\build.ps1` 与 `scripts\release.ps1`
  （.NET 自包含发布——免装 .NET 8，需装一次 Windows App SDK 1.6 运行时）；
  推送 `v*` 标签自动构建并创建 GitHub Draft Release。
- 已知限制：WinAppSDK 全自包含布局的 XAML 资源解析问题未解（启动即崩），
  v1.0.0 采用框架依赖 + 运行时前置说明绕行。
