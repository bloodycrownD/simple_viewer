# 更新日志（Changelog）

本文件记录用户可感知的变更。格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循语义化版本；历史起点为本文件引入（v1.0.0 之前的旧版单图查看器不追溯）。

## [1.0.3] - 2026-09-24

翻页闪错修复 + 缩略图应用管线重构。

### 修复

- **单图来回翻页报「加载失败」（参数错误 / 该对象已关闭）根治**：被替换下台的图像源
  在退役释放时会连带关闭它呈现过的位图，而该位图仍躺在解码缓存里——翻回近期看过
  的图片时缓存命中拿到已关闭位图，换源必炸。现缓存命中时体检位图健康度（已关条目
  自动重新解码），换源失败另有一次按陈旧重试兜底（覆盖体检与释放的竞态窗口）。

### 性能

- 缩略图应用改走磁盘缓存文件的异步图像管线（解码与纹理上传全在系统图像线程，
  界面线程零解码成本，实测单张应用 78ms → 0ms）；解码改位图直出 + 编码器直读
  （消除每张 0.5-2MB 大对象堆分配，千张冷启动不再有 GB 级分配压力）；新瀑布流
  布局安定后才开始成批应用（首帧呈现不受应用风暴挤占）。

### 诊断

- UI 心跳看门狗的计时器被垃圾回收静默停跳，导致每次启动约 15-20 秒处固定误报一条
  假「界面无响应」——计时器改持会话级引用；卡死现场自动抓取全部线程栈（外部诊断
  管道）并周期采样 GC 停顿数据（卡顿是否源于 GC 可直接从日志判定）；单图加载失败
  与换源位图异常现完整落诊断日志（含位图状态与路径）。

## [1.0.2] - 2026-09-23

双特性版本：筛选从「点选 OR」升级为条件树，标签管理补齐批量面。

### 新增

- **复杂筛选器（条件树）**：工具栏「筛选」按钮弹出筛选面板——标签条件支持
  「包含任一 / 不包含任一」（in / not in），「全部满足（且）/ 任一满足（或）」
  可嵌套组合（最深 3 层）；无标签条件入口保留。筛选条改表达式预览形态
  （条件胶囊、否定红显、括号分组），面板编辑实时生效。左栏标签点击保持
  Explorer 心智：无修饰 = 单选筛选（替换，再点取消），Ctrl+点击 = 加/减入筛选。
- **批量标签管理（两层模型）**：左栏标签明确为「配置层」——删除标签/组仅移除
  定义（0 文件改名），文件名上仍存在的标签自动落入新增的「未定义标签」区，
  可连锁删除（按文件真删）或收纳进组；配置删除前检查快捷键绑定引用（被引用
  则整体拒绝并提示）。
- **图库右栏（选中集面板）**：图库模式右栏显示选中图片的标签并集与每标签张数，
  chip ✕ 一键批量移除，「＋」打开批量目录打标（追加 / 仅未含 / 替换三态）；
  常驻可收展，收展改变瀑布流可用宽度。
- **工具栏左右分簇**：左簇为图库操作（全选切换 / 筛选 / 删除选中集），右簇为
  主题等全局操作；单图与图库模式各自只显示相关按钮。
- **删除选中集**：图库多选后一键送回收站（确认框 + 无 UI 批量删除 +
  失败明细聚合回执 + 索引同步清理）。
- **单图顶部信息横条**：返回图库按钮 + 序号 i/N + 文件名的整宽不透明横条，
  大图上按钮可读，顶部不再透光。

### 性能

- 根除滚动/拖拽期间 ≥15 秒的整窗冻结（缩略图应用后 UI 线程跑图像解码异步链
  与解码线程互等成死锁）：拖拽视觉改为按下时懒生成，生成链全程后台线程。
- 放大（≥1.2× 换全分辨率源）与翻页顿挫消除：解码直出位图（解码 + 降采样 +
  EXIF 全在图像管线线程、零中间拷贝），UI 线程只做 O(1) 换源；缓存改持单份位图。
- 未定义标签连锁删除的候选转换由 O(N×M) 线性扫描改预建字典 O(N+M)
  （全库量级标签不再卡 UI）；滚动期解码量减半。

### 修复与打磨（CR round2 + 九轮实机走查）

- 批量删除与批量打标互斥闸（并发改同一批文件会产出指向已删路径的幽灵卡片）；
  未定义区操作与目录打标的管线级异常兜底（错误经通知条回执，不再静默无响应）；
  对话框并发打开保护补齐。
- 左栏标签点击交互回归修复：v1.0.1 后左栏点击被改为「一律追加筛选条件」，
  本版恢复一贯的 Explorer 心智（无修饰 = 单选筛选替换、Ctrl+点击 = 加/减选）；
  左栏标签行与图库卡片补充读屏/自动化可达名称。
- 单图查看「返回悬空」修复（两层）：①图片比例与画布不匹配时，图片四周空白区与
  顶部横条底色不一致形成断层——画布层统一为与横条相同的不透明底色；②打标回执条
  （批量操作的失败/警告回执）常驻期间会把单图横条顶离工具栏悬在画布中部——回执条
  改为浮层显示，不再推移布局。
- 标签操作时闪退修复（两处）：①批量删除（删除选中集进回收站）的 Shell 调用误跑在
  线程池线程上，而回收站操作要求单线程单元（STA）——在大批量删除时引发原生堆损坏
  闪退，改为专用 STA 后台线程；②旧图像源（瀑布流缩略图、单图换源）被替换后交由 GC
  在后台线程释放，而此类 XAML 对象必须在界面线程析构——大图库打标重排时成批退休
  引发同类崩溃，改为界面线程确定性延迟释放。
- 卡死（界面完全冻结数十秒后恢复）修复：冷缩略图缓存风暴期（首次打开大图库、
  以及打标改名后缩略图需重新生成的场合），多张缩略图连续提交给渲染管线会使其
  长时间失联——应用段加入最小间隔节流。诊断能力同步增强：卡死自动现场转储、
  打标/筛选/缩略图管线分段耗时日志（startup.log 可见）。
- 图库右栏随画布同宽排布（布局列）、面板底色铺满、标签区结构对齐单图详情页、
  全部浮层统一不透明底色（深浅双主题）、通知条收起后的顶部暗缝消除。
- 验证脚本族加固：settings 备份还原防「崩溃残留毁好备份」窗口、路径改相对
  推导（任意机器可跑）；12 个实机验证脚本入库。
- 核心逻辑（标签投影 / 条件树求值 / 批量文件操作 / 索引批量删除）下沉 Core
  可测层，测试 100 → 120 项。

## [1.0.1] - 2026-09-19

首个补丁版本：v1.0.0 实机走查反馈的 UI 打磨与构建确定性收口。

### 修复与打磨

- 侧栏与标签目录弹窗：滚动条不再遮挡行右计数（ScrollViewer 右侧让位 14px）。
- 标签行首标记统一 radio/checkbox 隐喻：互斥组 = 单选圆点、兼容组 = 圆角方框
  （组色相描边，筛选激活/已含时实心）——此前兼容组无行首标记，视觉不一致。
- 单图右栏标签区：chips 改内容自适应 + 380px 限高滚动，标签少时不再出现
  中部大片空白（原为列表弹性行撑满剩余高度）。
- 发布脚本确定性：双 csproj 并行还原竞写 `project.assets.json` 的竞态定论，
  改串行还原 + 产物校验兜底；publish 补 `--no-restore`；发布目录先清空
  （防 XBF/程序集代际错配）；散装 XAML 资源（.xbf）与图标显式补拷。

## [1.0.0] - 2026-09-19

首个正式版本：从"单目录单图查看器"升级为"带标签的图库浏览器"。

### 新增

- **TagSpaces 文件名标签**：标签内嵌于文件名尾部方括号（`photo[风景 已修].jpg`），增删改全部
  通过重命名完成，不产生任何额外文件；与 TagSpaces 桌面版互通。
- **标签组体系**：互斥组（组内单选，替换对用户透明）与兼容组（组内可叠加）两种语义，
  组/标签支持新建、重命名、删除（执行前告知影响张数）；跨平台文件名长度预算预检
  （Windows 260 字符路径 / Linux 255 字节文件名双口径，超限阻止并提示）。
- **瀑布流图库**：递归扫描图库根目录、虚拟化滚动、按原始宽高比多列瀑布排布、
  缩略图按需异步加载与磁盘缓存；扫描渐进呈现（无需等全量完成），扫描中可随时操作。
- **多选与批量打标**：Explorer 心智选择（单击单选 / Ctrl 加减选 / Shift 范围 / Ctrl+A 全选）；
  拖拽图片到左侧标签行打标（拖拽集内任一卡 = 整集），拖拽视觉为小缩略图随鼠标，
  多选时悬停显示"打标 N 张"。
- **打标入口**：拖拽打标、单图详情右栏 chips（✕ 移除 / ＋ 目录选择器添加）、
  可视化快捷键打标（单图/选中集批量）。
- **标签筛选**：点标签筛选（单选替换心智，Ctrl 多标签 OR）；侧栏 ∅ 按钮一键筛选
  无标签图片（打标工作入口）；筛选条实时命中统计，chip 可逐个移除。
- **单图查看**：遮盖式布局（画布铺满整窗，侧栏/工具栏浮层遮盖）——收展侧栏图片位置
  纹丝不动、不闪不重载；滚轮锚点缩放、拖拽平移、双击复位、旋转、全屏、回收站删除、
  分类归档；左上"◀ 返回图库"按钮与 Esc/Enter 键盘路径。
- **单图详情右栏**：文件信息（文件名/大小/像素尺寸/序号）与标签 chips 管理。
- **主题三态**：深色（默认）/浅色/跟随系统，弹窗与浮层跟随。
- **启动体验**：无参数启动自动恢复上次图库根目录；CLI 支持 `viewer <文件>` 直开与目录参数。

### 性能

- 打标（重命名）不重载图片：同图改名保持旧源直到新源就绪，缓存键随路径迁移；
  成功回执静默（就地视觉反馈），仅失败弹回执（数量与原因）。
- 几十万张量级：索引（SQLite）与缩略图缓存为可丢弃缓存，打开图库清表重扫；
  解码走 WIC Fant 插值防缩放锯齿；EXIF 方向计入宽高比（竖拍卡片比例正确）；
  卡片回收释放位图内存；扫描与筛选/重开图库竞态收口。
- 同目录切图体感 < 100ms，冷启动 ≤ 2s（PRD 口径）。

### 修复与加固（CR round1）

- 扫描中重开图库/激活筛选的竞态与重复卡片问题收口。
- 卡片交互 Button 化（键盘与辅助功能路径可达）；图库 Enter 进入单图。
- 设置文件损坏（v1 迁移/非法字符）不再崩溃；标签名拒绝文件系统非法字符。
- 标签重命名部分失败、批量打标失败明细回执；诊断日志与 UI 心跳看门狗。
- 核心状态机（筛选/瀑布流布局/自然排序一致性）下沉 Core 并补自动化测试（100 项）。
- 瀑布流卡片行序回归修复：缩略图被钉死在 48px 细条（cr/P2-9 行序颠倒），恢复
  图片在上、文件名在下的正常卡片布局。
- 自包含发布包启动崩溃修复：publish 布局补齐散装 .xbf 与图标资源
  （此前报 XamlParseException 无法定位 MainWindow.xaml）。

### 构建

- 双 csproj 拆分（UI / 可单测 Core）；`scripts\build.ps1` 与 `scripts\release.ps1`
  （.NET 自包含发布——免装 .NET 8，需装一次 Windows App SDK 1.6 运行时）；
  推送 `v*` 标签自动构建并创建 GitHub Draft Release。
- 已知限制：WinAppSDK 全自包含布局的 XAML 资源解析问题未解（启动即崩），
  v1.0.0 采用框架依赖 + 运行时前置说明绕行。
