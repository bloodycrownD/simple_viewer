# tag-filter-tree 开发编排状态（code-dev-loop）—— dev-ready
# spec: docs/iterations/图片打标签与瀑布流浏览/features/tag-filter-tree/spec.md

spec_path: docs/iterations/图片打标签与瀑布流浏览/features/tag-filter-tree/spec.md
prd_path: demo/filter.html（设计稿，非标准 PRD）
branch: feature/tag-filter-tree
base_sha: b8b2511
head_sha: 302e648
status: dev-ready
spec_summary: 纯标签 in/not in + and/or 条件树（≤3 层），单一内存求值器（索引层零改动），TagFilterState 重构为条件树（Core），MainViewModel 状态收敛，左栏 QuickAdd，Flyout 非模态面板（组=卡片嵌套），表达式 chips，不落盘

dag_version: 1
wave_plan: [[impl-core],[verify-core],[cr-func-core],[impl-pipeline],[impl-entry],[impl-panel],[impl-integration],[verify-full],[cr-func-final]]
node_status:
  impl-core: { status: done, head_sha: 2265379+db70b5c, executor: subagent }
  verify-core: { status: done, executor: main }                    # 107/107
  cr-func-core: { status: done, func_ready: yes }
  impl-pipeline: { status: done, head_sha: 3160952, executor: subagent }
  impl-entry: { status: done, head_sha: 3d499e8, executor: main }  # trivial
  impl-panel: { status: done, head_sha: 4707797+d3f22e5, executor: subagent }
  impl-integration: { status: done, head_sha: 3c02fea+e117ac5, executor: subagent }
  verify-full: { status: done, executor: main }                    # 冷重建成功 + 107/107
  cr-func-final: { status: done, func_ready: yes }
open_must_fix: []
spec_deviations:
  - id: DEV-1
    status: closed
    desc: 值选择「＋标签」行内展开（非二级 Flyout）——spec R1 明文降级预案（嵌套 Flyout 触发父级 light-dismiss）
  - id: DEV-2
    status: open   # 留 Step 8 用户拍板：接受两态切换按钮组即 closed；要求 demo select 形态则按集中替换点还原（BuildTwoStateSwitch/MakeSwitchOption + L114/L233）
    desc: 组头「全部/任一」与条件行「包含任一/不包含任一」为两态切换按钮组（非 ComboBox 下拉）——语义等价、交互同构，视觉形态与设计稿 select 有差；动机=Flyout 内二级 popup 主题风险
notes:
  - Step 8（manual_user，spec blocking:no）留用户实机走查：demo 形态还原度/双主题/10 万级体感/拖拽打标回归/快捷键抑制体感/扫描中面板实时性
  - RULE.md Flyout 先例待实机双主题实证后落录（终检建议）
  - 构建产物：Debug bin 已随各波次生成，可直接实机走查
